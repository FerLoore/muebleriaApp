import api from './api';

// TODO: Implementar métodos del servicio para Entrega
export const getEntrega = async () => {
  // const response = await api.get('/Entrega');
  // return response.data;
};
