import api from './api';

// TODO: Implementar métodos del servicio para OrdenCompra
export const getOrdenCompra = async () => {
  // const response = await api.get('/OrdenCompra');
  // return response.data;
};
